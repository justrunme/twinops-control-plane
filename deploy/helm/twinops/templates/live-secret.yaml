{{- if and .Values.live.enabled .Values.live.apiToken }}
apiVersion: v1
kind: Secret
metadata:
  name: twinops-live-api
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: twinops-live
type: Opaque
stringData:
  api-token: {{ .Values.live.apiToken | quote }}
{{- end }}
