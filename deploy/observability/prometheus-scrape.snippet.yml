# Snippet to merge into a Prometheus scrape_configs list.
# Targets a local/demo TwinOps live API.
- job_name: twinops-live
  metrics_path: /metrics
  scrape_interval: 5s
  static_configs:
    - targets: ["127.0.0.1:8080"]
      labels:
        service: twinops-live
